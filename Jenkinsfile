@Library("Shared") _
pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script {hello()}
                script {hellworld ("hello world of sajjad")}
                
            }
        }
    }
}
