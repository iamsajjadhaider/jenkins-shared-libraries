@Library("Shared") _
pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script {hello()}
                script {hello_world ("hello world of sajjad")}
                
            }
        }
    }
}
