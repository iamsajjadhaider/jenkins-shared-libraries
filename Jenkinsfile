@Library("Shared") _
pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script {hello()}
                script {helloworld ("hello world of sajjad")}
                
            }
        }
    }
}
